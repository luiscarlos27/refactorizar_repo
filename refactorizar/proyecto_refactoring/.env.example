# Copia este archivo como .env y define tus valores reales.
# El archivo .env NO se versiona (esta en .gitignore).

# Clave de la API de OMDB (obtenela gratis en https://www.omdbapi.com/apikey.aspx)
# Requerida: la aplicacion no arranca sin ella.
OMDB_API_KEY=
